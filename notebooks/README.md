# Notebooks

Exploration only — anything that graduates into the pipeline moves to
`apexodds/` with tests. Keep heavy dumps out of git (`data/` is ignored).

Suggested starters:
- `01_fastf1_explore.ipynb` — one race end-to-end through `apexodds.data.normalize`
- `02_tyre_curves.ipynb` — fitted vs prior degradation per circuit category
- `03_replay_anatomy.ipynb` — win-prob time series vs race events for one GP
